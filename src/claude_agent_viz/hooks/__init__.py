"""Hooks module - deprecated.

This module previously contained code for installing Claude Code hooks.
The new implementation uses file-based discovery instead, which doesn't
require any hook installation.
See the discovery module for the current implementation.
"""
